
  <header>
    <nav class="navbar navbar-inverse navbar-static-top navbar-custom">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">TWITLITICS</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/profile">My Profile</a></li>
        <% if(isLoggedIn) { %>
          <li><a href="/users/<%= currentUser.id %>/favorites">Favorites</a></li>
        <% } %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if(isLoggedIn) { %>
          <li><a href="/logout">Logout</a></li>
        <% } else { %>
          <li><a href="/login">Login</a></li>
        <% } %>
        <% if(!isLoggedIn){ %>
        <li><a href="/signup">Sign Up</a></li>
        <% } %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->

<div class="container-fluid">
<!-- Brand and toggle get grouped for better mobile display -->
<div class="navbar-header">
  <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false" style="visibility: hidden;">
    <span class="sr-only">Toggle navigation</span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
  </button>

</div>

<!-- Collect the nav links, forms, and other content for toggling -->
<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <ul class="nav navbar-nav">
    <li><a href="/featured">Featured Users</a></li>
    <li><a href="/tweets">Top Tweets</a></li>
    <li><a href="/hashtags">Top Hashtags</a></li>
  </ul>

  <ul class="nav navbar-nav navbar-right">
    <form class="navbar-form navbar-left" id="search-bar" method="get" action="/users/">
      <div class="form-group">
        <input type="text" class="form-control" id="search-input" placeholder="Search">
      </div>
      <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-circle-arrow-right"></button>
    </form>

    <script type="text/javascript">
      var searchForm = document.getElementById('search-bar')

      searchForm.addEventListener('submit', function(evt) {
        evt.preventDefault()
        var query = document.getElementById('search-input').value
        window.location.href = '/users/' + query
      })
    </script>

  </ul>
</div><!-- /.navbar-collapse -->
</div><!-- /.container-fluid -->
</nav>

  </header>

    <div class="jumbotron jumbotron-fluid">
  <div class="container">
    <% if(isLoggedIn) {%>
    <h1 class="display-3"><%= currentUser.local.user_name %></h1>
    <p class="lead">I'm a Twitlitics badass</p>
    <% } else { %>
      <h1 class="display-3">Welcome</h1>
      <p class="lead"><i class="fa fa-smile-o"></i></p>
    <% } %>
  </div>
</div>
